check process mongodb with pidfile <%= node[:monit][:mongo][:pidfile] %>
  group database
  start program = "service mongod start"
  stop program = "service mongod stop"
  if failed host 127.0.0.1 port 27017 then restart
  if 15 restarts within 15 cycles then timeout
